<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.product.mapper.ProductReviewMapper">
	<select id="productReply"
	resultType="com.exam.product.vo.ReplyVO" parameterType="int">
	SELECT p3.rev_no, p3.star_point, p3.rev_title, p3.rev_create, p3.rev_content, p3.user_id, p3.pay_no
	FROM PRODUCTS p JOIN PAYMENTS p2 ON p.prod_no = p2.prod_no
                    JOIN PRODREPLY p3 ON p2.pay_no = p3.pay_no
	WHERE p.prod_no = #{prod}
	</select>

</mapper>