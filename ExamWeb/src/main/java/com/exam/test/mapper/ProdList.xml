<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.test.mapper.ProdListMapper">
 <select id = "prodList" resultType="test">
   SELECT prod_no,
          prod_name,
          price,
          content,
          create_date,
          image,
          pdf,
          count,
          user_id,
          category_no
	FROM products
 </select>
</mapper>